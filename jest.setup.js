// This file can be used for global Jest setup
// For example, mocking global objects or setting up test-specific environment variables

// Example: Mocking Next.js Image component (if needed for other tests, not strictly for these)
// jest.mock('next/image', () => ({
//   __esModule: true,
//   default: () => {
//     return 'NextImageMock';
//   },
// }));

// Silence console.error and console.log during tests if they are too noisy
// (Use with caution, might hide important warnings)
// beforeEach(() => {
//   jest.spyOn(console, 'error').mockImplementation(jest.fn());
//   jest.spyOn(console, 'log').mockImplementation(jest.fn());
// });
